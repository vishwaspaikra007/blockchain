// pragma solidity 0.5.0;

// contract PatientData {

//     uint256 public DonationAmount = 0;
//     mapping(address => uint) public donors;

//     function IncrementDonationCount(uint amt) public {
//         donors[msg.sender] += amt;
//         DonationAmount += amt;
//     }
  
// }
